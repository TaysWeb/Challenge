
function calculateGrade() {
    const marks = [39 , 42, 65, 71, 84, 93] ;

    // declaring my counter variable and additional variable 
    let i = 0;
    let total = marks.length([i]);

    // using the if statement by displaying the symbols 

    if ( marks[0] <= 60 , i++ ) {           //value
    console.log("Grade is F ")  ; 
    }
    if ( marks[1] <= 70 , i++ ) {              //value
    console.log("Grade is D ")  ; 
    }
   if ( marks[2] <= 80 , i++ ) {               //value
    console.log("Grade is  C ")  ; 
   }
   if ( marks[3] <= 90 , i++ ) {              //value
    console.log("Grade is B ")  ; 
   }
   if ( marks[4] <= 100, i++ ) {              //value
    console.log("Grade is A ")  ; 
}
}

function calculateAverage() {
   let (i += total([i])) ;
   console.log((marks.length) / 2) ;
}